{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "comment": "SPA fallback so /quote/homeowners and /review/auto reach React instead of 404ing at the CDN. The negative lookahead keeps /api/* out of it -- without that, every serverless function would be served the HTML shell instead of running.",
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
